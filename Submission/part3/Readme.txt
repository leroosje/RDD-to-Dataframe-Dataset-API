Each of the 10 files has in it:

The function that was been written in scala,

Tests that are written using scala syntax, that are intended to run the code written in scala.

Tests written using Spark SQL, to give a baseline for our functions to be compared to

And a shell script that has been provided to aid in running.


Each file is named for the function being changed.